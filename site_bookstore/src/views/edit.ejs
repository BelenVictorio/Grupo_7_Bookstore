<!DOCTYPE html>
<html>
  <%- include('partials/head', {
      title: 'Paginas Bellas || Edit'
  }) %>
  <body>
    <%- include('partials/header') %>
    <main class="main-creaEdit">
        <section class="creaEdit-section">
            <form action="/products/update/<%= product.id %>?_method=PUT" method="POST" class="creaEdit-form">
                <h2>Editar producto: <%= product.name %></h2>
                <div class="creaEdit-div">
                    <input type="text" id="titulo" name="name" placeholder="Titulo" value="<%= product.name %>">
                    <label for="titulo"></label>
                </div>
                <div class="creaEdit-div">
                    <input type="text" id="autor" name="author" placeholder="Autor/a" value="<%= product.author %>">
                    <label for="autor"></label>
                </div>
                <div class="creaEdit-div">
                    <textarea id="descripcion" name="description" placeholder="Descripcion"><%= product.description %></textarea>
                    <label for="descripcion"></label>
                </div>
                <div class="creaEdit-div">
                    <label for="Categorias"></label>
                    <select name="category" id="Categorias">
                    <option value="">-Seleccione la categoria correspondiente</option>
                    <option value="adultos" <%= product.category === "adultos" && 'selected' %>>adultos</option>
                    <option value="jovenes" <%= product.category === "jovenes" && 'selected' %>>jovenes</option>
                    <option value="infantil"<%= product.category === "Infantil" && 'selected' %>>infantil</option>
            </select>
                </div>
                <div class="creaEdit-div">
                    <input type="text" id="precio" name="price" placeholder="Precio" value="<%= product.price %>">
                    <label for="precio"></label>
                </div>
                <div class="creaEdit-button">
                    <button type="submit">
                        <a href="/">Cancelar</a>
                    </button>
                    <button type="submit" name="editar">Editar</button>
                    
                </div>
            </form>
        </section>
    </main>
    <%- include('partials/footer') %>
  </body>
  
</html>